# Week 4 empty-stub backup (Checkpoint 1)

These files are **copies of the minimal stub** versions (docstring + empty `__all__` only, no `src` imports).

- Use the parent `layers/*.py` for **normal development** (re-export from `src/`).
- To submit **strict empty stubs**, copy over the repo root `layers/*.py`:

```bash
cp layers/_archive_week4_empty_stubs/world_model.py layers/world_model.py
# … repeat for semantic, causal, motivation, language
```

Or keep the re-export version if your grader accepts it.
