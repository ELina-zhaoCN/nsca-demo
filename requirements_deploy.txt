# Streamlit Community Cloud deployment requirements
# Excludes mujoco (requires native build tools not available on cloud runners)
# and wandb (not needed for the read-only demo UI).
#
# Streamlit Cloud uses this file when STREAMLIT_REQUIREMENTS_FILE is set,
# or when configured in the App Settings → Advanced → Python requirements path.

# Core ML
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Data / Science
numpy>=1.21.0
pandas>=1.5.0
scikit-learn>=1.0.0

# Visualization / UI
matplotlib>=3.5.0
streamlit>=1.28.0

# Pre-trained models
transformers>=4.30.0
timm>=0.9.0
open-clip-torch>=2.20.0

# Image / Audio
pillow>=9.0.0
opencv-python-headless>=4.5.0
librosa>=0.10.0

# Config / utilities
pyyaml>=6.0
tqdm>=4.60.0
gdown>=5.0.0

# LLM integration (optional — Layer 4)
openai>=1.0.0

# Testing
pytest>=7.0.0
