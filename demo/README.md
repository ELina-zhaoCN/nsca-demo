# Demo (GitHub Issue #2 layout)

The Streamlit application entrypoint is **`app.py` at the repository root** (shared with `README.md`).

From the repository root, after activating your virtual environment:

```bash
streamlit run app.py
```

Or use the helper script (also from repository root):

```bash
bash demo/run_demo.sh
```
